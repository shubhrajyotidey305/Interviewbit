int Solution::reverse(int A) {
    
    long long rev = 0;
    
    while(A!=0){
        rev = A%10 + rev*10;
        if(rev>INT_MAX || rev<INT_MIN) return 0;
        A /= 10;
    }
    
    return rev;    
}
